#include <bits/stdc++.h>

#define MAC0214 std::ios_base::sync_with_stdio(false);
#define coxa std::cin.tie(NULL);
#define int long long
using namespace std;

void solve(){
    int n; cin >> n;
    string s; cin >> s;
    int l = 0, r = 1, ans = n, err = 1;
    int lim = n/2;
    lim = ((n & 1)? lim-1 : lim);
    while(l < r/2){
    }
}

signed main(){
    MAC0214 coxa
    int t; cin >> t;
    while(t--) solve();
    return 0;
}
