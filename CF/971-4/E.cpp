#include <bits/stdc++.h>

#define MAC0214 std::ios_base::sync_with_stdio(false);
#define coxa std::cin.tie(NULL);
#define int long long
using namespace std;

void solve(){
    int k, n; cin >> k >> n;
    int l = k, r = k+n-1;
    int m1 = l + (r-l)/3;
    int m2 = r + (r-l)/3;
    while(true){
        if()
    }
    return;
}

signed main(){
    MAC0214 coxa
    int t; cin >> t;
    while(t--) solve();
    return 0;
}
